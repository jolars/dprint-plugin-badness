{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Configuration",
  "description": "dprint-facing configuration, serialized as camelCase.\n\nThe enum-valued options are stored as `String` and parsed lazily, borrowing\ntheir JSON schema from the formatter's own enums (its `schema` feature) so\nthe published `schema.json` enumerates badness's accepted values instead of\nhand-listing them here. Those wire values are `badness.toml`'s kebab-case\nspellings (`single-line`), so the two config files agree on everything but\nthe key casing.",
  "type": "object",
  "properties": {
    "lineWidth": {
      "description": "Maximum line width the layout engine targets. Defaults to dprint's\nglobal `lineWidth`, or 80 if unset.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0,
      "default": 80
    },
    "indentWidth": {
      "description": "Number of spaces per indentation level. Defaults to dprint's global\n`indentWidth`, or 2 if unset. badness always indents with spaces, so\ndprint's global `useTabs` has no effect.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0,
      "default": 2
    },
    "itemIndent": {
      "description": "How continuation lines in list items are indented from the `\\item`\ncolumn.",
      "$ref": "#/$defs/ItemIndent",
      "default": "hang"
    },
    "wrap": {
      "description": "How to lay out line breaks inside a paragraph. When unset, each file kind\nuses its own default — `.tex` reflows; `.sty`, `.cls`, `.dtx`, `.ins`,\nand `*.code.tex` are code, so they preserve authored breaks.",
      "anyOf": [
        {
          "$ref": "#/$defs/WrapMode"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "mathWrap": {
      "description": "How to lay out line breaks inside display math. `auto` derives from the\neffective `wrap`.",
      "$ref": "#/$defs/MathWrap",
      "default": "auto"
    },
    "lineEnding": {
      "description": "How the formatted line breaks are spelled. Defaults to dprint's global\n`newLineKind`, or `auto` (keep what the file was written with) if unset.",
      "$ref": "#/$defs/LineEnding",
      "default": "auto"
    },
    "lang": {
      "description": "Document language as a BCP-47-style code (`en`, `de`, `pt-BR`, …), used\nby the `sentence` and `semantic` wrap modes to pick the\nsentence-boundary abbreviation profile. Ignored by other wrap modes.",
      "type": [
        "string",
        "null"
      ],
      "default": null
    },
    "noBreakAbbreviations": {
      "description": "Extra no-break abbreviations for the `sentence` and `semantic` wrap\nmodes, keyed by language code or the literal `default` bucket. An\nabbreviation listed here never ends a sentence.",
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "default": {}
    }
  },
  "additionalProperties": false,
  "$defs": {
    "ItemIndent": {
      "description": "How continuation lines in a list item are indented from the `\\item` column.",
      "oneOf": [
        {
          "description": "Align continuations under the body following a bare `\\item `. The default.",
          "type": "string",
          "const": "hang"
        },
        {
          "description": "Indent continuations by one `indent_width` step.",
          "type": "string",
          "const": "indent"
        },
        {
          "description": "Align continuations with the `\\item` command.",
          "type": "string",
          "const": "none"
        }
      ]
    },
    "WrapMode": {
      "description": "How the formatter lays out line breaks inside a paragraph.\n\nThe sentence-boundary detection behind [`WrapMode::Sentence`] and\n[`WrapMode::Semantic`] is a per-language abbreviation profile\n(`formatter::sentence`); the language and any user no-break abbreviations are\nresolved from config into the [`SentenceOptions`](super::SentenceOptions)\nthreaded through the lowering.",
      "oneOf": [
        {
          "description": "Greedy fill: pack words up to `line_width`, breaking only where the next\nword would not fit. The default.",
          "type": "string",
          "const": "reflow"
        },
        {
          "description": "Preserve acceptable authored breaks and redistribute only the smallest\nregion needed to satisfy `line_width` and approach the soft equilibrium\ntarget ([`FormatStyle::stable_wrap_target`]). Aimed at keeping revision\ndiffs small: a small prose edit perturbs the smallest possible region.",
          "type": "string",
          "const": "stable"
        },
        {
          "description": "Wrap after each sentence (one sentence per line). Line width is ignored — a\nlong sentence stays on one line.",
          "type": "string",
          "const": "sentence"
        },
        {
          "description": "Semantic line breaks (<https://sembr.org/>): keep the author's soft line\nbreaks *and* add a break after each sentence. Like [`WrapMode::Sentence`]\nplus preserving authored newlines; clause boundaries survive only where the\nauthor placed a break (no comma/colon detection).",
          "type": "string",
          "const": "semantic"
        },
        {
          "description": "Leave paragraph line breaks exactly as authored (only collapse trailing\nwhitespace and blank-line runs, as before reflow existed).",
          "type": "string",
          "const": "preserve"
        }
      ]
    },
    "MathWrap": {
      "description": "How a single-formula *display-math* body (`\\[…\\]`, `$$…$$`, a non-grid\n`equation`) is line-broken. Grid environments (`align`, `gather`, matrices)\nand inline `$…$` are unaffected.",
      "oneOf": [
        {
          "description": "Derive from the resolved [`WrapMode`]: [`WrapMode::Preserve`] gives\n[`MathWrap::Preserve`], every other wrap mode gives [`MathWrap::Break`].\nThe default.",
          "type": "string",
          "const": "auto"
        },
        {
          "description": "Keep the author's line breaks inside the body. Content within each\nauthored line is still normalized (operator spacing, script tightening),\nand lines sit at the body indent.",
          "type": "string",
          "const": "preserve"
        },
        {
          "description": "Never insert breaks: the body stays on one line, overflowing the line\nwidth if too long (matching inline math's behavior).",
          "type": "string",
          "const": "single-line"
        },
        {
          "description": "Break a too-long body before its top-level binary/relation operators\n(amsmath style).",
          "type": "string",
          "const": "break"
        }
      ]
    },
    "LineEnding": {
      "description": "The byte sequence the formatter's line breaks render as.\n\nThe layout engine always builds output with `\\n` (the printer is the sole\nauthority on *where* breaks go); this only selects how those breaks, and the\nones carried through from the source, are spelled in the final string. The\nconversion is document-wide — protected regions included, since a `verbatim`\nbody whose line terminators disagreed with the rest of the file would be a\nmixed-ending document (see the invariant note in\n`docs/src/development/architecture.md`, § *The formatter*).",
      "oneOf": [
        {
          "description": "Keep what the source used: `\\r\\n` if the document's first line break is a\nCRLF, `\\n` otherwise. The default, so formatting never rewrites a file's\nline endings behind the author's back.",
          "type": "string",
          "const": "auto"
        },
        {
          "description": "Always `\\n` (Unix).",
          "type": "string",
          "const": "lf"
        },
        {
          "description": "Always `\\r\\n` (Windows).",
          "type": "string",
          "const": "crlf"
        },
        {
          "description": "`\\r\\n` on Windows, `\\n` everywhere else.",
          "type": "string",
          "const": "native"
        }
      ]
    }
  }
}
